# 对于字符串类型的一些方法：

indexOf("字符串内容", 开始引索)  其返回为改查找字符串的第一字母引索；

对象.substring(开始引索， 结束引索) 结束引索默认为该字符串的结束；

## 对于开始于结束内容的判断：

startsWith / endsWith

该方法接受参数为字符串，用于判断开始或者接受是否为该字符串，返回boolean类型。

对象.replaceAll("要替换对象"，"新对象")

对于字符串长度

对象.length  返回字符串长度

取出字符串一个值  charAt(引索)

## 去除空格

对象.trim();

## 对于字符串的分隔：

split("a:分隔字符串")； 其参数意义为，在字符串里查找  a  并用  a  进行分割；



## 大小写转换：

toUpperCase()   [全部变成大写] /  toLowerCase()   [全部变为小写]

eg:

```java
public static void main(String[] args){
  String text = "姓名|年龄|性别\n张三|20|男\n李四|18|男\n小花|18|女";

  // 使用 split 进行换行符的分割，得到一个新的数组对象
  String[] data = text.split("\n");

  // 因为第一行是标题不是数据，所以我们需要把长度-1
  // (注意要使用小括号包围，因为要先计算长度再组合字符串)
  System.out.println("共有:"+(data.length-1)+" 条记录");

}
```

eg:

```java
  public static void main(String[] args) {
    String text = "ZhanSan";
    // 把拼音全部转化为大写字母
    String enName = text.toUpperCase();
    System.out.println(enName);
  }
```

## 字符串的比较：

对象.equals("被比较的字符串")；  来判断两字符串是否向同；

eg:

```java
public static void main(String[] args) {

    String text = "字符串";
    // 使用 equals 方法判断是否相同
    if (text.equals("字符串")) {
      System.out.println("equals 方法字符串相等");
    }
    // 前后顺序无所谓,下面代码是一样的
    if ("字符串".equals(text)) {
      System.out.println("equals 方法字符串相等");
    }
  }
```

## 对于数字于字符串的转换  Integer.parseInt("字符串内容")

eg:

```java
public static void main(String[] args) {
    String text = "123";
    // 转化字符串为数字
    int a = Integer.parseInt(text);
    System.out.println(a);

    // 转化字符串为数字
    a = Integer.parseInt("100");
    System.out.println(a);
  }
```

