# 网络编程：

## 协议：

协议是网络协议的简称，网络协议是通信计算机双方必须共同遵循的一组约定。比如怎么互相认识，怎样建立链接，只有遵循整个协议，计算机之间才能相互通信。

## HTTP/HTTPS 协议：

目前网络上有两种协议：HTTP与HTTPS

![image-20250412083224793](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250412083224793.png)

对于HTTP 其传输以明文为主，端口为80；无证书，安全性差；对于HTTPS 其以加密传输为主，端口为443，使用证书，加密安全，很难劫持。

## 对于 URL  就是望门常说的网址。其格式：

![URL格式](https://style.xxx.com/img/ham/course/py2/py2-1-2.png?x-oss-process=image/resize,w_1024/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

协议类型  ://  域名  /路径(中间每层都是以  /  分隔)(路径相当于一层层的文件夹)/  ?  参数(多个参数间用&进行分隔)  参数用 Key = value  的格式表示

在一些网址中，端口后会跟  :443 或者  :80  这个是端口号，一般进行省略。

## Get请求 - 无参数：

Java可以进行网络抓包。

安装依赖库：Okhttp3  是一个非常流行的HTTP库 ,可以简单，快速的实现  HTTP  调用。

## 安装 Okhttp3  的方法是在  pom.xml  文件里中增加依赖：

```xml
<!-- https://mvnrepository.com/artifact/com.squareup.okhttp3/okhttp -->
<dependency>
 <groupId>com.squareup.okhttp3</groupId>
 <artifactId>okhttp</artifactId>
 <version>4.1.0</version>
</dependency>
```

唯一的  <dependencies>  标签里，包含多个  <dependency>  标签，每一个  <dependency>  标签代表一个依赖库。书写时注意嵌套顺序。

### 对于  pom.xml  文件：

其作用就是定义java  项目中所需要的依赖库。

## 使用  Okhttp3  完成页面请求：

1，实例化  OkHttpClient 。使用

OkHttpClient okHttpClient = new OkHttpClient();    代码。

2，执行调用。

  (1), 在执行调用之前，需要实例化一个  Request  对象，作用是定义请求的各种参数。

Request request = new Request.Buiilder().url(url).build();

  (2), 然后构建调用对象。

Call call = okHttpClient.newCall(request);

  (3), 最后执行调用，如果调用失败可能抛出异常，所以必须抓取异常。call.execute()  就是调用的代码。

3，execute() 返回其实是一个执行内容的结果对象，调用对象的方法及即课获取返回值的字符串内容：

call.execute().body()..string();

完整的一个代码：

eg：

* ```java
   pubic class GetPage(){
  
      public String getContent(String url){
  
   ​        OkHttpClient okHttpClient - new OkHttpClient();
  
   //  定义一个请求  .  配置  .  url
  
   ​    Request request = new Request.Builder().url(url).build();
  
   ​    Call call = okHttpClient. newCall(request);
  
   ​    String result = null;
  
   ​    try{
  
   ​        result = call.execute().body().string();
  
   } catch (IOException e){
  
   ​    System.out.println("request" + url + "error");
  
   e.printStackTrace();
  
   }
  
   return result;
  
   }
  ```

      public static void main(String[] args) {
    
      ​    GetPage getPage = new GetPage():
    
      ​    String url = "XXXX";
    
       String  content = getPage.getContent(url);
    
      System.out.println("call" + url + "content" + content);
    
      }
    
      }
    
      eg：
    
      package com.xxx.test.http;
    
      import okhttp3.Call;
      import okhttp3.OkHttpClient;
      import okhttp3.Request;
    
      import java.io.IOException;
    
    
    
    
      public class GetPage {
    
        /**
    
         * 根据输入的url，读取页面内容并返回
           */
      ```

```java
public String getContent(String url) {

OkHttpClient okHttpClient = new OkHttpClient();
Request request = new Request.Builder()
        .url(url)
        .build();

Call call = okHttpClient.newCall(request);
// 返回结果字符串
String result = null;
try {
  result = call.execute().body().string();
} catch (IOException e) {
  System.out.println("request" + url + "error");
  e.printStackTrace();
}

return result;
```

  }

```java
  public static void main(String[] args) {
    GetPage getPage = new GetPage();
    String url = "https://www.done.kim/api/m/musicRankings";
    String content = getPage.getContent(url);
```

```java
System.out.println("API调用结果");
System.out.println(content);
```

  }
}

## 对于输出的结果：

call.eecute().body().string();

会返回服务器的具体内容。我们在  console  中看得见的大部分内容都是给给浏览器识别的内容，很多时候为你们都要使用程序调用**API ** 获取数据。

## 对于API：

API（应用程序接口）API  一般指一些预先设定的函数，目的是可以为开发人员快速解决某一程序，而无需了解和访问源码，或理解他内部工作机制与细节。

###### API可以快速调用某个程序

比如：

https://api.seniverse.com/v3/weather/daily.json?key=SCYrvkytJze9qyzOh&location=杭州

这个可以直接调用出杭州的2023-05-09的天气情况的数据，省略了精美的网页。

API  其实也是一个  URL  而其返回内容统称为数据。

## 上面我们练习了在程序里调用API  获取结果，很多情况下，API  需要调用参数。

## 对于  POST  表单数据：

提交数据至服务端进行增加，修改，删除等操作，都是  POST 操作。  我们在网络上提交表单进行登录的场景，其实就是典型的  POST  操作

### 在java里实现  POST  操作：

#### post()  方法：

okhttp3  库里支持  POST  操作，在之前学习的调用  API  属于 GET  操作。不同的是,  POST  操作时，数据不是放在  URL  里，而时放在表单里。

所以程序实现需要一个  FormBody  表单对象，用于放置表单数据：（核心代码）：

```java
Builder builder = new FormBody.Builder();
// 设置数据，第一个参数是数据名，第二个参数是数据值
builder.add("", "");
FormBody formBody = builder.build();

Request request = new Request.Builder().url(url).post(formBody).build();
```

对于之前的方法：

```java
new Request.Builder().url(url).post(formBody).build();
```

属于连写，拆分开为：

```java
Request.Builder build = new Request.Builder();
build = build.url(url);
build = build.addHeader("Referer", "http://www.taobao.com");
build = build.post(formBody);
Request request = build.build();
```

想完全理解  url header post  的知识，需要学习《计算机原理》，《计算机网络》。

对于  post()  代码示例：

```java
package com.xxx.test.http;

import java.io.IOException;
import java.util.Map;
import java.util.HashMap;
import okhttp3.Call;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.FormBody;
import okhttp3.FormBody.Builder;

public class FormPoster {

  public String postContent(String url, Map<String, String> formData) {
    // okHttpClient 实例
    OkHttpClient okHttpClient = new OkHttpClient();   //Client  的中文意思是客户端。
```

```java
//post方式提交的数据
/**/Builder builder = new     FormBody.Builder();
// 放入表单数据
for (String key : formData.keySet()) {
  builder.add(key, formData.get(key));
}/**/上面代码主要实现数据格式的转换
// 构建 FormBody 对象
FormBody formBody = builder.build();
// 指定 post 方式提交FormBody
Request request = new Request.Builder()
        .url(url)
        .post(formBody)
        .build();

// 使用client去请求
Call call = okHttpClient.newCall(request);
// 返回结果字符串
String result = null;
try {
  // 获得返回结果
  result = call.execute().body().string();
} catch (IOException e) {
  // 抓取异常
  System.out.println("request " + url + " error . ");
  e.printStackTrace();
}
return result;
}
```

```java
  public static void main(String[] args) {
    String url = ;
    Map<String, String> formData = new HashMap();   //这行代码定义了一个名为 “formData” 的变量，它的类型是 “Map<String, String>”，即一个键值对都是字符串类型的映射（Map）。使用 “HashMap” 作为具体的实现类来创建这个映射对象。这样的映射可以用来存储具有字符串键和字符串值的一组数据，例如存储表单数据时，键可以是表单字段的名称，值可以是对应字段的值。
    formData.put("tel", );
```

```java
FormPoster poster = new FormPoster();   //“FormPoster” 可能是一个用于提交表单数据的类。
String content = poster.postContent(url, formData);

System.out.println("API调用结果");
System.out.println(content);
  }
}
```

#### 表单数据以键值对形式呈现的原因

在 HTML 表单中，每个表单元素（如输入框、下拉框、复选框等）都有一个 `name` 属性，这个 `name` 属性的值就是键，而用户在该表单元素中输入或选择的值就是对应的值。当表单被提交时，浏览器会将所有表单元素的键值对组合起来，发送给服务器。

#### 键值对的具体含义

可以把键理解为变量名，值理解为变量内容，以下是详细解释：

- **键（Key）**：通常是表单元素的 `name` 属性值，它是一个标识符，用于在服务器端标识和区分不同的表单字段。例如，在一个登录表单中，用户名输入框的 `name` 属性可能设置为 `username`，密码输入框的 `name` 属性可能设置为 `password`，这里的 `username` 和 `password` 就是键。
- **值（Value）**：是用户在表单元素中输入或选择的具体内容。比如用户在用户名输入框中输入了 `john_doe`，在密码输入框中输入了 `123456`，那么 `john_doe` 和 `123456` 就是对应的值。

## POST JSON 数据：

## 对于  JSON添加依赖：

- ```java
  <!-- JSON 操作库 -->
          <dependency>
              <groupId>com.alibaba</groupId>
              <artifactId>fastjson</artifactId>
              <version>1.2.62</version>
          </dependency>
  ```

- ### 对于 JSON 与 FastJSON :

https://ham.xxx.com/articles/detail/5f3757fd5e205f30b2c2b1f9

### 在开发过程中，除了提交  表单数据  外，也经常提交  JSON数据  。

与  表单数据  一样也是调用 post() 方法，只是参数不再使用  FormBody  对象，而是使用  RequestBody

假设存在数据![image-20250415131202404](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250415131202404.png)

eg：

```java
package com.xxx.test.http;

import java.io.IOException;
import java.util.Map;
import java.util.HashMap;
import okhttp3.Call;
import okhttp3.OkHttpClient;
import okhttp3.Request;
import okhttp3.FormBody;
import okhttp3.FormBody.Builder;

public class JsonPoster {
//定义JSON文件字符码号
    private static final MediaType JSON_TYPE = MediaType.parse("application/json; charset = utf-8");

  public String postContent(String url, Map<String, String> formData) {
    // okHttpClient 实例
    OkHttpClient okHttpClient = new OkHttpClient();   //Client  的中文意思是客户端。
```

```java
//直接转换json类型
String param = JSON.toJSONString(formDate);
//将其放入要提交的数据当中
RequestBody requestBody = RequestBody.create(JSON_TYPE, param);
// 指定 post 方式提交requestBody
Request request = new Request.Builder()
        .url(url)
        .post(requestBody)
        .build();

// 使用client去请求
Call call = okHttpClient.newCall(request);
// 返回结果字符串
String result = null;
try {
  // 获得返回结果
  result = call.execute().body().string();
} catch (IOException e) {
  // 抓取异常
  System.out.println("request " + url + " error . ");
  e.printStackTrace();
}
return result;
}
```

```java
  public static void main(String[] args) {
    String url = ;
    Map<String, String> data = new HashMap();   
    data.put("key", "value");  //添加键值对
```

```java
requestPoster poster = new FormPoster(); 
String content = poster.postContent(url, formData);

System.out.println("API调用结果");
System.out.println(content);
  }
}
```

## 对于 Response - 网页：

对于上节使用  OkHttp3  进行API调用，使用了语句：

```java
call.execute().body().string()
```

execute()  方法真正执行发送请求，前面的一系列代码都是前置准备。这段代码获得服务器返回内容的专用代码，不具有通用性。

事实上，除了具体的返回结果，我们通常还担心的数据是  http  状态码；

状态码是用一个数字直接反应本次请求的状况，例如常见的  200  表示请求成功，  404  表示出错了，服务器端还没有被请求内容。具体状态码内容如下https://ham.xxx.com/articles/detail/5f3758675e205f30b2c2b2a4

## 为了响应状态码,可以使用一下语句：

```java
call.execute().code()
```

但是一般要读取响应内容，但是又不能执行两次请求，所以代码应该优化为：

```java
import okhttp3.Response;

// 执行请求
Response rep = call.execute();
// 获取响应状态码
int code = rep.code();
// 获取响应内容
String content = rep.body().string();
```

## 在上我们学习用 OkHttp3  进行状态码的返回与文本的返回。但实际  OkHttp3  库不仅可以进行请求网页，API，也可以请求图片，excel  等各种文件。

图像文件的明显不同是：图像的内容不是可以直接阅读的字符文本，而是二进制编码，需要软件解析图片二进制码数据，才能还原图像显示。

对于解析图片，excel  等文件的时候不能用

```java
reponse.body().string();
```

而是利用：

```java
response.body().bytes();
```

获取二进制编码

## Response - JSON:

前面我们用 OkHttp3库  进行响应码与返回，无论是文本文件还是图片，excel 的内容。而工作的时候，经常会调用API，而很多API返回的文本内容是JSON格式。

JSON 是一段文本，也是java的字符串，是难以解析具体内容的，所以必须转换成Java对象。

前期我们利用  fastjson  库把参数数据转换为  JSON  格式字符串，当然可以把  JSON 结果转换为对象，方便程序今后一步分析。

### JSON  文本主要是在网页上做交互的，现在我们需要把他转换为对象，方便程序做处理

格式：

```java
Map contentObj = JSON.parseObject(content, Map.class);
```

### 解析JSON对象：

在实际工作中，对于返回值为 JSON 格式的 API ，我们不仅要学习如何调用 API 更需要解析：
例如：

```json
{
  "status": "success",
  "country": "China",
  "countryCode": "CN",
  "region": "ZJ",
  "regionName": "Zhejiang",
  "city": "Hangzhou",
  "zip": "",
  "lat": 30.294,
  "lon": 120.1619,
  "timezone": "Asia/Shanghai",
  "isp": "Chinanet",
  "org": "Chinanet ZJ",
  "as": "AS4134 Chinanet",
  "query": "115.227.217.8"
}
```

我们可以发现上述JSON 格式被大括号包裹，里面是一串串的键值对，

所以可以  Map content = JSON.parseObject([调用内容], Map.class);

还有例子就是：

```json
{
  "code": 0,
  "data": {
    "ip": "117.89.35.58",
    "country": "中国",
    "area": "",
    "region": "江苏",
    "city": "南京",
    "county": "XX",
    "isp": "电信",
    "country_id": "CN",
    "area_id": "",
    "region_id": "320000",
    "city_id": "320100",
    "county_id": "xx",
    "isp_id": "100017"
  }
}
```

可以转化成：

```java
Map contentObj = JSON.parseObject(content, Map.class);
Map dataObj = (Map)contentObj.get("data");
String city = (String)dataObj.get("city");
```

可以注意一下

```java
Map dataObj = (Map)contentObj.get("data");
```

我们的  contentObj  为Map 对象， 但是其  get("data")  方法为返回 String 类型，而从数据上来看，我们希望将  data  数据转换为Map类型，所以利用（Map）强制类型转换。

因为 Map 可以储存任何对象，所以从  Map  中  get()  到的对象必须指定其实际类型：(Map) , (String)

# JSON解析网址：

https://www.bejson.com/jsonviewernew

## User_Agent:

对于一些情况，我们利用OkHttp3  进行网页  API  的请求时，不一定每次都可以成功，

对于某些大型网站，出于安全考虑，都会对每一次请求进行校验，判断请求是否来自浏览器的访问。

如过不是来自真实的浏览器，API  服务器任务不是真实的浏览器请求，就直接拒绝掉了

## 对于判断请求来源：

判断是否真实来源浏览器，需要在  HTTP  消息头  (Headers)  中获得User-Agent  消息后才能判断。

## HTTP header:

HTTP 消息头 Headers 是  Http  协议的一项重要内容，作用是在发起请求的时候，除了请求参数外，可以附加更多信息（moremore inform）：

https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Headers

主网址：

https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Reference

对于  Headers  信息并不是写在  URL  中，而是被隐藏，无法直接看到。

## User-Agent（Agent : 代理）：

User-Agent 是放在Headers 中的一种数据信息。作用是在指定  URL  发送信息请求的时候，告诉服务器当前用户的浏览器类型，版本等数据。

**因此** 我们只要在程序代码里面附加上 User-Agent 信息就可以继续发送请求啦。

**当然** 这里的 User-Agent  是模拟的；在  win7 + chrome  环境里  User-Agent  的写法格式：

```html
Mozilla/5.0 (Windows NT 6.1; WOW64) AppleWebKit/535.1 (KHTML, like Gecko) Chrome/14.0.835.163 Safari/535.1
```

## 对于该  User-Agent  信息的附加方法：

在  OkHttp3库  中已经存在方法：

```java
Request request = new Request.Builder()
    .url(url)
    .addHeader("User-Agent", "")
    .build();
```

## Referer:

上节内容学习设置  User-Agent  解决了程序请求中  HTTP  不成功的情况，还有一类更严格的检测：图片防盗链。

## 如何防盗的呢：

因为浏览器在请求网页中的图片（或其他任何文件）是，会自动在  HTTP  消息头  Headers  中加一个  Referer  信息，表示亲求的来源（可以理解为图片的上级网页）

比如有一个网址图片，他在其他的一些地方是打不开的，是因为在从乙方进行图片访问的时候，会自动在  HTTP 的小消息头加上  Referer  信息，表示请求来源（自动添加  Referer  是业界规范，默认行为，不容易干涩），即浏览器自动告诉图片服务器，当前请求是在乙方发的，这时图片服务器拒绝访问，因为图片服务器不允许其他网址访问图片

## 为什么直接粘贴网址可以访问

因为直接拷贝网址，粘贴到浏览器地址栏，就没有“来源”了。此时不是在网页里面加载图片，而是在浏览器里面直接加载，图片就没有上级了。（浏览器不是上级，浏览器是浏览网页的工具而已。换句话说，“来源”，“上级”指的是访问的具体内容）

User-Agent  就表示用什么浏览工具。

图片服务器允许无  Referer信息  时访问，就可以看见图片了。也就是说，决定权仍然在“图片服务器”上，不同公司的图片服务器不一样不能一概而论。（浏览器只是收集一些信息提交到服务器上，并不能决定是否能看见图片）

## 程序里的  Referer:

为了模拟浏览器自动加  Referer信息 的行为，可以调用以下程序。

```java
Request request = new Request.Builder()
    .url(url)
    .addHeader("Referer", "https://ham.xxx.com/course/j14/0")
    .build();
```

但是加了  Referer信息  ，程序访问此图片也是失败的，响应码为403

## 解决方法：

一劳永逸的方法就是将  Referer信息  改为原始网站，在后期会学习如何利用工具获取  Referer值

## HOST：

前两节我们学习了  OkHttp3库  设置HTTP协议的 Headers  ，以此解决了调用网址，API过程中可能出现的各种问题，

这节学习  HOST  也是  Headers  中的重要信息。

HOST  表示当前请求的域名。虽然中国域名已经存在于 URL 里，但是遇到复杂的场景，例如使用了代理服务器，或者  URL 里不屑域名而是写入  IP 地址进行请求 ，设置 HOST 就非常有用了。

HOST  的作用比较宽泛，就不能确定各种网站的服务器会拿  HOST  做什么，**下面是  HOST的解析网站**

  https://developer.mozilla.org/zh-CN/docs/Web/HTTP/Headers/Host

## 跟  User-Agent  和  Referer  一样  HOST  也是属于  Headers 数据的字段之一：

如代码：

```java
Request request = new Request.Builder()
    .url(url)
    .addHeader("Host", "www.douban.com")
    .build();
```

（注意，HOST  的值一定是一个域名而且不带协议头）

## 下载文件：

上面，我们学习了如何进行图片，excel等类型的文件请求，此类二进制文件无法在console中进行查看，所以请求结果必须写入文件。

## 在  java  文件里写文件必须经历3个步骤;

创建文件对象——> 写入内容——> 关闭写入操作。

## 写入文本文件：

FileWriter

```java
import java.io.File;
import java.io.FileWriter;

// 文件对象
File file = new File("foo.txt");

// 写入内容
FileWriter fileWritter = new FileWriter(file.getName());
fileWritter.write(content);

// 关闭
fileWritter.close();
```

## 写入二进制文件：

FileOutputStream

```java
import java.io.File;
import java.io.FileOutputStream;

// 文件对象
File file = new File("china-city-list.xlsx");

// 写文件
FileOutputStream fos = new FileOutputStream(file);
fos.write(data);

// 必须刷新并关闭
fos.flush();
fos.close();
```

## 下载图片与下载二进制文件一样，都属于  byte[]  数据

## 解析  excel  ：

前面我们学习了如何下载文件，下载后点击文本文件，图片，都可以直接查看其内容。另一种常见的文件是 excel文件，但是正常情况下需要  office 软件才能读取。

实际上，  Java 已经有库可以操作  excel文件了。

## easyexcel依赖库：

easyexcel 是快速，简单操作  excel 文件的库。使用前必须在  pom.xml  文件加入依赖：

```xml
<dependency>
  <groupId>com.alibaba</groupId>
  <artifactId>easyexcel</artifactId>
  <version>3.1.1</version>
</dependency>
```

## 调用库：

excel 文件多为 sheet模式，每个sheet 实际上就是一个表格。表格分**行列**

所以parse数据路径一定是：sheet——> 行——> 列

```java
import com.alibaba.excel.EasyExcel;
import java.util.Map;
import java.util.List;

// 读取第一个sheet
List<Map<Integer, String>> sheetDatas = EasyExcel.read("xzq_201907.xlsx").sheet(0).doReadSync();
// List 中每个元素表示一行
for (Map<Integer, String> rowData : sheetDatas) {
  // Map 中用序号指代每一列
  for (Integer index : rowData.keySet()) {
    // 列值
    String columnValue = rowData.get(index);
  }
}
```

## 解读：

parse文件的第一步就是读取文件内容，调用：

EasyExcel.read() 方法，传入文件名称，然后这里解析第一个工作表，所以调用 sheet()  方法。传入参数  0  .最后的 doReadSync()  表示同步方式读取文件内容。返回一个读取到的内容集合 List 。这是一个连贯的写法。

返回的 List 集合里，系统用 Map 类表示一行数据（因为系统不知道 excel 对应的具体对象，其实 Map 可以当作一种通用的对象）

![示意图](https://style.xxx.com/img/ham/course/py2/j14-5-3-1.svg)

## 自动转换为类：

在不能提前确定 excel 文件每一列的含义是，或者复杂场景下 excel 文件的列，经常变化，用 Map 表示每一列数据比较好。

但是如果知道 excel 文件每一列的含义，用自定义类来表示，就很好

```java
// 读取第一个sheet
List<DemoData> sheetDatas = EasyExcel.read("xzq_201907.xlsx").head(DemoData.class).sheet(0).doReadSync();
```

这里多调用一个方法： .head(DemoData.class)， DemoData 就是自定义的类，表示一行的数据，类的每个属性都表示一列的值

返回值为 List<DemoData> 就表示把每一行都转换为DemoData 的实例对象，放进 List 集合里。

```java
// 属性定义的顺序必须与列顺序保持一致
public class DemoData {
  private String code1;
  private String city1;
  private String code2;
  private String city2;
  private String code3;
  private String city3;
}
```

![示意图](https://style.xxx.com/img/ham/course/py2/j14-5-3-2.svg)

# **cookie:**

前面我们学习 okhttp3  库 可以调用 API，抓取网页，下载文件。但是这些操作都是不需要登录的，如果API ，网页，文件内容要求登录才能访问，就要学习新的 cookie 相关知识了。

所谓 **cookie**，是储存在客户端浏览器中的一段文本内容。以 key = value （数据名称，数据值）的格式储存一条数据；多余数据间用分号；分开。

由于各种浏览器都对 cookie 大小和数量有限制，所以 cookie 目前的核心功能就是储存登录数据；额外可以储存点小数据，比如  ：

```java
是否登录 = true；呢称 = 张三  
```

这样内容不大的数据。

假设我们对豆瓣进行操作:

### 1,前期准备：

首先登录我的豆瓣

必须先找到登录后的cookie

### 2，找到 cookie

打开浏览器（比如Goole）的开发者模式，切换到Networkk 菜单栏，

开发则模式：https://www.cnblogs.com/yaoyaojing/p/9530728.html

在Network菜单栏的子菜单**Headers**里找到mine请求(如果没有mine，就选择第一个跟浏览器地址蓝相同的URL请求)再在mine请求里把cookie的值拷贝放在cookie.txt文件里。

接下来我们要编码把Cookie的值转换成Map类型数据。

其实cookie的值也是存在Headers数据里面的

## session（会话）:

上节，我们用开发者工具抓取了登录时的cookie，然后使用程序完成登录。

但是 cookie 的弊端是， cookiue 是存放在客户端浏览器的，而且是临时的，登录后还想以登录的状态与服务器通讯，就比较麻烦。

这个问题需要 session（会话） 对象来解决这个问题。

### 1，假设用时光网进行登录后：

### 2，搜集信息

​    打开浏览器开发者工具，**Preserve log**必须勾选。

### 关键点：输入用户名和密码后，在右侧抓取到的网络请求中，获得到登录请求API

![时光网3](https://style.xxx.com/img/ham/course/py2/j14-6-2-3.png?x-oss-process=image/resize,w_1024/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

### 对于新版浏览器：

在表头后面多了一个“载荷”（Payload）的菜单

![时光网3](https://style.xxx.com/img/ham/course/py2/j14-6-2-6.png?x-oss-process=image/resize,w_1024/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

切换到小菜单就可以看见密码：

![时光网3](https://style.xxx.com/img/ham/course/py2/j14-6-2-7.png?x-oss-process=image/resize,w_1024/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

### 对于一些网站，其内部需要输入login进行过滤

## 操作一般流程：

1，登陆地址

2，执行方式（GET/POST）

3，HOST

4，Referer

5，User-Agent

6，表单数据，数据名称和数据值

```java
  // 用 CookieJar 实现 cookie 的存储，便于登录后请求其它 URL 可以复用
  private static final OkHttpClient okHttpClient = new OkHttpClient.Builder()
      .cookieJar(new CookieJar() {
        private final HashMap<String, List<Cookie>> cookieStore = new HashMap<>();

        @Override
        public void saveFromResponse(HttpUrl url, List<Cookie> cookies) {
          cookieStore.put("mtime.com", cookies);
          System.out.println("[saveFromResponse]url.host()=" + url.host());
        }

        @Override
        public List<Cookie> loadForRequest(HttpUrl url) {
          System.out.println("[loadForRequest]url.host()=" + url.host());
          List<Cookie> cookies = cookieStore.get("mtime.com");
          return cookies != null ? cookies : new ArrayList<>();
        }
      })
      .build();
```

## HashMAp：

cookieStore 的类型是 HashMap，相当于一个登录信息的**集合容器**，可以用来存不同域名的登录信息（cookie）。

## 代码实例：

```java
cookieStore.put("mtime.com[需要储存的网站域名]", cookies)；
```

这个代码是把当前爬取到底时光网的登录信息储存起来。

```java
cookieStore.get("mtime");
```

这个代码是把爬取的时光网登录信息从Store容器（临时容器）里拿出来。

* static 表示类变量，意味着无论 new 多少个 PageLoginer 对象 PageLoginer.okHttpClient 都只有一个

* final 表示 okHttpClient 一旦第一次 呢哇出现对象后，不能再次 new 新对象。
