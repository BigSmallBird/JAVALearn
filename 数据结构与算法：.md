# 数据结构与算法：

## 二分法：

每次进行一步操作，让我们的筛选范围或者目标范围缩小一倍。

## 大O记法：

编程基本上是在于数据打交道，大多数编程基本上是处处理**获取数据，查询数据，操作数据，返回数据**相关的逻辑。

数据结构预算法就是为了更快更省的处理数据。

**即减少空间复杂度**

### 对于空间复杂度判断：

* 测试结果大大依赖硬件条件。
* 测试结果需要事后才能计算。
* 测试结果受原始数据特性影响大。

**sum up：**

所以我们需要一个不用具体测试数据和测试环境，就可以粗略地估计算法执行效率的方方法。这个方法我们叫做复杂度，即**大O记法**。大O记法是数据结构于算法的基石。

## 时间复杂度：

我们使用**步数**作为时间复杂度的计算。步数就是**数组的每次索引的读取**，就算一步，即 **unit_time**.

### 案例：数组值的获取

![img](https://style.xxx.com/img/course/a1/2/2-2-1.svg)

我们获得第四个数组元素值。

```java
int a[] = {0, 1, 2, 3, 4, 5, 6};
System.out.println(a[3]); // step1
```

如上 step1 ，获取 a 第三个索引位置的值1，我们把这个时间称为第一步，**计算机可以通过一部跳到任意一个索引的为主进行数据读取**，这个时间也是单位时间：**unit_time**。用大O记法叫做 ---O(1)**。（数组长度任意）。

**若x为数组长度。y为n * unit_time 则图像：**

![img](https://style.xxx.com/img/course/a1/2/O1.png?x-oss-process=image/resize,w_800/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

对于**二分法 (O(logN)) **进行猜数字游戏:

![img](https://style.xxx.com/img/course/a1/2/Olog.png?x-oss-process=image/resize,w_800/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

### 计算 N 的阶乘：

题目：定义一个函数，这个函数可以计算传入参数 N的阶乘 N！。

即 ： f(N) = N * (N - 1) * (N - 2) ....

```java
public int factorial(int n) {
    int result = 1; //step1
    for (int i = 1; i <= n; i++) { //step2
        result *= i;  // step3
    }
    return result;
}
```

#### 对于这个时间复杂度：

![image-20250420110840907](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420110840907.png)

即 ：**---O(2N + 2)**

对于这个算法，要使用2次历遍 即2 * N的 unit-time 进行for循环，2次变量初始化 而这种情况下的线性关系，我们通常记作： **O(N)**。

### 计算一个数组的所有组合方式：

对于组合式算法：例如

```java
public void combine(String args[]) {
    for (int i = 0; i < args.length; i++) { //step1
        for (int j = i + 1; j < args.length; j++) { //step2
            System.out.println(args[i] + args[j]); //step3
        }
    }
}
```

分析其时间复杂度：![img](https://style.xxx.com/img/course/a1/2/2-2-5.svg)

即 **N^2 + N + 1** 的非线性时间 记作**O(N^2)**

这几个时间复杂度的图像：

![img](https://style.xxx.com/img/course/a1/2/ON2.png?x-oss-process=image/resize,w_800/watermark,image_d2F0ZXJtYXNrLnBuZz94LW9zcy1wcm9jZXNzPWltYWdlL3Jlc2l6ZSx3XzEwMA==,t_60,g_se,x_10,y_10)

### Sum UP：

对于 **大O记法**，只保留最大趋势公式： 指数 > 线性 > 对数 > 常数。

#### 所有对于时间复杂度，我们只需要看**for循环的次数**

## 空间复杂度：

与时间复杂度一样，同样遵循大O记法

时间复杂度以**步作为基础单位**。 空间复杂度以一个**基础数据类型**值当作基础单位。

O(1)

```java
int a = 0;
int j = 0;
```

O(N)

```java
int a[] = new int[n]
```

### 优先考虑时间复杂度：

在编程过程中，我们有限考虑时间复杂度，因为我们默认计算机的内存是足够大的。（**摩尔定律**：当价格不变时，集成电路上课容纳的元器件的数目，每隔18~24个月会翻一番，性能提升一倍。）

对于参与嵌入式开发啊，参与单片机相关编程开发，才需要注意空间问。

## 性能优化小案例1- 二分法查找：

对于一个数组，我们希望从里面查找某个数字是否存在。

### 线性查找：

历遍整个数组暴力破解。

![image-20250420113706150](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420113706150.png)

```java
public static int find(int[] array, int aim) {
    for (int i = 0; i < array.length; i++) {
        if (aim == array[i]) {
            return i;
        }
    }
    return -1;
}
```

记： O(N)。

二分法查找

![image-20250420113751316](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420113751316.png)

```java
public static int find(int[] array, int aim) {
    // 初始化left = 最左侧, right = 最右侧
    int left = 0;
    int right = array.length - 1;

    // 当left > right则表示遍历完成
    while (left <= right) {
        // 获取中间的值
        int middle = (left + right) / 2;
        int middleValue = array[middle];
        if (middleValue == aim) {
            // 已经找到目标
            return middle;
        } else if (middleValue < aim) {
            // 目标在middle的右侧，重置left
            left = middle + 1;
        } else {
            // 目标在middle的左侧，重置right
            right = middle - 1;
        }
    }
    return -1;
}
```

#### 小注意：

对于字符串的大小比较方法：

```java
str1.compareTo(str2)
```

如果返回值为负数，即 str1 < str2

## 性能优化小案例2-二次问题：

假设给定一个数组，数组里面的数字介于 0 ~ 10 之间，找出**重复的数字**；

### 暴力破解法：

获取每个元素，依次判断整个元素与后续元素是否重复。![image-20250420122451467](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420122451467.png)

```java
public static ArrayList<Integer> repeat(int[] array) {
    ArrayList<Integer> result = new ArrayList<>();
    for(int i = 0; i < array.length; i++){
        // 以此判断i位置元素和后面j位置元素是否相等
        for(int j = i + 1; j < array.length; j++){
            if(array[i] == array[j]){
                // 如果之前result没有，则进行添加
                if(!result.contains(array[i])){
                    result.add(array[i]);
                }
            }
        }
    }
    return result;
}
```

O( N^2 );

## 标记法：

这个题目要求我们知道数组里面每个数字都是介于 0 ~ 10 之间。我们是否可以先用11 长度的数组标记0 ~ 10 这11 个数是否存在；null 表示不存在，1 表示一次，2表示2次。

### 如何判断重复？

一旦出现数字就将 exists 对应的索引值 + 1. 下次如果遇见对应的位置为 > 1 表示重复。

#### 步骤：

前置![image-20250420123359001](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420123359001.png)

我们首先申请一个数组，数组名称为exists，表示**数字是否存在**数组长度为11，并且数组里面的默认值为0，表示 0 ~10 这 11 个数默认都不存在。

### 第一步：![image-20250420123612707](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420123612707.png)

我们扫描到数字0，因此我们将 exists 数组中的索引值为0的数组设置为1. exists[0] = 1.

### 第二步：![image-20250420123807026](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420123807026.png)

我们扫描到数字8，因此我们将 exists 数组引索值为8 的数字设置为1。  exists [8] = 1; 以此类推。

### 第六步：

我们的exists数组0，2，3，5，8索引位置已经设置为1.![image-20250420185148940](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420185148940.png)

我们扫描到原始数组的第6位位2，发现exists数组索引为2的位置已经为一，所以我们知道2这个数字重复了。

```java
public static ArrayList<Integer> repeat(int[] array) {
    ArrayList<Integer> result = new ArrayList<>();
    int[] exists = new int[11];
    for (int i = 0; i < array.length; i++) {
        int value = array[i];
        // 只有当前位置已经为1，标示重复，并且输出，>1情况则不输出了
        if (exists[value] == 1) {
            result.add(value);
        }
        // 用exists标示记录
        exists[value]++;
    }
    return result;
}
```

对应此方案的的时间复杂度为 **O(N)** 。

### 注意：

### 字符串获取字符：

对于**字符串有用索引提取其单个元素的犯法  charAt(索引)  **比如：

```java
String str = "abcdse";

str.cahrAt(0) = "a";

str.cahrAt(3) = "d"
```

### ASCLL编码:

a 对应的ASCLL编码为 97 并且 b，c，d以此增加，则有：

```javaj
‘b’  -  'a' = 1;

'c' - 'a' = 1;
```

## 计算机内存管理：

### 关于数组索引都从 0 开始：

**数组是计算机中最基础的数据结构**

#### 数组存储：

**定义**：数组是一种线性表数据结构。他用一组连续的内存空间，来储存**相同类型的数据**。

### 数组读取：

每个数组都有对应的内存地址，我们将其称为 **start_address**.

在定义时，我们已经确定了数组类型，即**元素需要大小**—— **item_size.**

### 则数组的每个元素地址计算方式：

```java
// 第一个元素地址
start_address

// 第二个元素地址
start_address + item_size * 1

// 第三个元素地址
start_address + item_size * 2
    
// 第N个元素地址
start_address + item_size * (N - 1)
```

**由此我们可以得到时间复杂度  O(1)**

**对于数组索引以0开始：为了方便计算地址  start_address + item_size * 0**

## 数组插入：

数组的插入是如何进行的？对于王一个数组里面插入数据的速度，取决于你需要把它插入那个位置。

**假设如下**：

```java
eat breakfast  // 吃早饭
shopping       // 购物
have lunch     // 吃午饭
have dinner    // 吃晚饭
```

突然我想在 Schedule 里添加上看电影怎么办:  那么我们计算其尾部地址：**start_address + item_size * this.size().**![image-20250420194935209](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250420194935209.png)

**对于代码实现：**

```java
public class Schedule {
  ......
  // 末尾插入
  public void add(String task){
    this.array[this.size] = task;
    this.size++;
  }
}
```

### 中间插入：

如果我们想在吃完午饭后添加给**下午茶**怎么办：我们需要在数组中间部分插入数据，但是为了**保证数组的顺序和连续的空间地址**，插入后面的数据需要依次后移。

对于此种**插入的时间复杂度**为 **O(N)**,对于此的代码实现：

```java
// 第三个位置插入
public void insert3Position(String task) {
  // 索引值为3的地方
  int index = 3;
  // 第一步：从右侧开始依次右移
  for (int i = this.size - 1; i >= index; i--) {
    this.array[i + 1] = this.array[i];
  }  //从后往前移；
  // 第二步：插入元素
  this.array[index] = task;
  // 调整size
  this.size++;
}
```

### 如果数组的空间长度不够怎么办？

那么我们就：

* 开辟新空间。
* 复制原来的数组到新的内存空间。
* 再进行插入操作。

### 数组的删除:

数组的删除与插入刚好相反

对于中间删除。南无我们就把元素右边的元素以此左移。

### 对于我们想让自定义ArrayList的长度无限制：

* 每次插入元素的时候我们判断元素的个数是否储存到上限。
* 如果达到上限，则创建一个新的数组（大小可以设置为原来的2倍）
* 接下来拷贝原来的数组。
* 再继续执行添加数组的操作。

### 冒泡排序：

* 指向数组相邻的元素（最开始是数组的头两个元素），并且**比较他俩的大小**
* 如果前者比后者大，ze**交换他们的位置**
* 如果后者比前者大,则**不交换**
* 然后依次排序后移，每次循环将最大的元素移动到最后一个位置。

#### 假设有一个数组 [9 ,2 ,4 ,7 ,5 ,3 ] 我们希望自小而大的排序。![image-20250421203125005](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250421203125005.png)

![image-20250421203151387](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250421203151387.png)

![image-20250421203206537](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250421203206537.png)

我们继续开始第二次循环，再次回到**第一个和第二个元素**![image-20250421203323856](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250421203323856.png)

![image-20250421203337230](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250421203337230.png)

#### 对于代码：

```java
// 冒泡排序
public static void bubbleSort(int[] array) {
    // 1. 每次循环，都能冒泡出剩余元素中最大的元素，因此需要循环 array.length 次
    for (int i = 0; i < array.length; i++) {
        // 2. 每次遍历，只需要遍历 0 到 array.length - i - 1中元素，因此之后的元素都已经是最大的了
        for (int j = 0; j < array.length - i - 1; j++) {
            //3. 交换元素
            if (array[j] > array[j + 1]) {
                int temp = array[j + 1];
                array[j + 1] = array[j];
                array[j] = temp;
            }
        }
    }
}
```

### 选择排序：

对于选择排序重点是**选择**，每次在剩余数组中，**选择最大的或则最小的哪个**，放在数组的一端。

* 利用两个变量，一个储存最大值，一个储存当前最大值的索引。
* 依次比较后面的元素，如果发现笔当前最大值大，则更新最大值与最大值的索引。
* 直到历遍结束，将最大值放在数组的最右边，
* 重复上述操作。

![image-20250422124736322](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250422124736322.png)

![image-20250422124802174](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250422124802174.png)

#### 对于改方法的时间复杂度可以计算得：

#### N + (N - 1) + (N -2) .... + 2 + 1 = O(N^2)

冒泡排序和选择排序：虽然时间复杂度都一样，但很显然，选择排序速度大于冒泡排序。因为冒泡排序在每一次比较后都进行一次位移。

## 插入排序：

重点在**插入**，每次抽离一个元素当作临时元素，依次比较和移动之后的其他元素，最终将这个临时元素插入对应的位置。

* 在第一轮，抽离数组末尾倒数第二个元素，作为临时元素。
* 用临时元素与数组后买你的元素进行对比；如果后面的元素值小于临时元素，则后面的元素左移。
* 如果后面的元素大于临时元素，或者已经移动到数组的末尾，则将临时元素插入当前空隙。
* 重复操作。

#### 第一次迭代：![image-20250425171341782](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425171341782.png)

#### 第二次迭代：![image-20250425171359659](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425171359659.png)

#### 第三次迭代：![image-20250425171421301](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425171421301.png)

每次排序都将**末尾部分数字排序好**

![image-20250425171525728](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425171525728.png)

## 二分插入排序：

**类似于在一个有序数组中查询，找到元素应该插入的位置**![image-20250425172951738](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425172951738.png)

第二次迭代，相当于在第一次迭代结果[3, 5]中寻找 7 因该插入的位置。第四次迭代，相当于在第二次迭代结果 [3, 5, 7]中寻找 4 应该插入的位子。

```java
// 查找应该插入的索引位置
public static int searchIndex(int[] array, int left, int right, int aim) {
    // 循环查找节点位置
    while (left < right) {
        int middle = (left + right) / 2;
        int value = array[middle];
        if (value < aim) {
            left = middle + 1;
        } else {
            right = middle - 1;
        }
    }
    // #1. 如果最终元素仍然大于目标元素，则将索引位置往左边移动一个
    if(array[left] > aim){
        return left -1;
    }
    // 否则就是当前位置
    return left;
}
```

![image-20250425173332840](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425173332840.png)

#### 插入排序优化：

```java
// 插入排序
public static void insertSort(int[] array) {
    // 从倒数第二位开始，遍历到底0位，遍历 N-1 次
    for (int i = array.length - 2; i >= 0; i--) {
        // 存储当前抽离的元素
        int temp = array[i];
        int index = searchIndex(array, i + 1, array.length - 1, temp);

        // #1. 根据插入的索引位置，进行数组的移动和插入
        int j = i + 1;
        while (j <= index) {
            array[j - 1] = array[j];
            j++;
        }
        array[j - 1] = temp;
    }
}
```

### 对于递归：

**自身续断的循环自身，就是递归**，递归是一个难点，同时也是算法基础。很多复杂的算法问题，如果你用递归思考。他会变得简单。

**递归在编程里就是自己嗲用自己**

比如阶乘 n！ = 1 * 2 * 3 * 4 *..... * n -1 * n

```java
public static int factorial(int n) {
    int factorial = 1;
    for (int i = 1; i <= n; i++) {
        factorial *= i;
    }
    return factorial;
}
```

### 递归的实现：

对于一个数学问题

f(1) = 1

f(n) = f(n - 1) * n

对于第二个式子，就是递归的思想。

```java
public static int factorial(int n) {
    //#1. 当 n = 1 时，递归结束
    if (n == 1) {
        return 1;
    }
    //#2. 把 factorial(n - 1) 的结果和 n 相乘，剩下的交给 factorial(n - 1) 来解决。
    return n * factorial(n - 1);
}
```

## 汉诺塔思想

.....

## 对于汉诺塔问题的实现:

### 对于递归如何用代码实现：

### 1，基准条件

即递归结束条件。

### 2，思考在基准条件下，会出现什么情况。

### 3，思考基准条件前一步情况，或者函数执行情况。

### 4，递归公式

解决了递归基准条件，我们需要考虑递归如何进行下去。也就是**递归公式**

### 分治思想：

分而治之思想采用了递归的思想，将原问题分成几个规模较小但是类似于原问题的子问题，通过递归的方式来求解这些小问题，最后将子问题的解合并来得到原来的问题的解。![image-20250425182514246](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425182514246.png)

### 归并排序：![image-20250425182552614](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425182552614.png)

![image-20250425182621375](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425182621375.png)

![image-20250425182630916](C:\Users\15956\AppData\Roaming\Typora\typora-user-images\image-20250425182630916.png)

对于代码的实现；

**1,如何递归进行数组的拆分  2，如何用原数组创建两个子数组**

```java
// 归并排序，返回排好序的数组
public static int[] mergeSort(int[] array) {
}

// 拷贝原数组的部分内容，从 left 到 right
public static int[] subArray(int[] source, int left, int right) {
}
```

```java
// 拷贝原数组的部分内容，从 left 到 right
public static int[] subArray(int[] source, int left, int right) {
    // 创建一个新数组
    int[] result = new int[right - left];
    // 依次赋值进去
    for (int i = left; i < right; i++) {
        result[i - left] = source[i];
    }
    return result;
}
```
